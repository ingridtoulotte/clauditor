# Project Rules

- Run `pnpm test` before every commit; fix failures before pushing.
- Format code with Prettier (2-space indent) on save.
- Write commit messages in Conventional Commits format.
- Branch from `main`; open a PR for every change.
- Keep functions under 50 lines; extract a helper otherwise.
